package result;

// I don't know if I am going to use this, but for a brief moment, I felt like I needed it. I will probably figure it out.

public class ClockEvent {
    String type; // like shotclock violations?

    public ClockEvent(String type) {
        this.type = type;
    }
}
